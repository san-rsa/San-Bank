import type { Principal } from '@dfinity/principal';
export interface _SERVICE {
  'add' : (arg_0: number) => Promise<undefined>,
  'balance' : () => Promise<number>,
  'compoundInterest' : () => Promise<undefined>,
  'sub' : (arg_0: number) => Promise<undefined>,
}
